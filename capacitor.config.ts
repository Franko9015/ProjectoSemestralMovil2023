import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'RegistraApp.com',
  appName: 'RegistraApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
