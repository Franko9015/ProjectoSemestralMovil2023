import { ComponentFixture, TestBed } from '@angular/core/testing';
import { OlvidastecontraseniaPage } from './olvidastecontrasenia.page';

describe('OlvidastecontraseniaPage', () => {
  let component: OlvidastecontraseniaPage;
  let fixture: ComponentFixture<OlvidastecontraseniaPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(OlvidastecontraseniaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
